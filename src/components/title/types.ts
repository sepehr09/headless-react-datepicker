export type TTitleProps = {
  /**
   * @default "numeric"
   */
  yearFormat?: "numeric" | "2-digit";

  /**
   * @default "short"
   */
  monthFormat?: "numeric" | "2-digit" | "long" | "short" | "narrow";
};
